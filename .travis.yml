language: cpp
compiler:
  - gcc
  # - clang # :TODO: assimp doesnt build under clang
os:
  - linux
script:
  - mkdir -p $TRAVIS_BUILD_DIR/build && cd $TRAVIS_BUILD_DIR/build && cmake .. && make
  - cd $TRAVIS_BUILD_DIR/bin && ./unit-tests
notifications:
  email:
    on_success: never
    on_failure: never
